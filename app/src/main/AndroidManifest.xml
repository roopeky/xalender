<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lkesovellus">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lääkesovellus">
        <activity
            android:name=".Activities.MemoActivity"
            android:exported="true" />
        <activity
            android:name=".Activities.DrugInfoActivity"
            android:exported="true" />
        <activity
            android:name=".Activities.DrugAddActivity"
            android:exported="true" />
        <activity
            android:name=".Activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".classes.ReminderBroadcast" />
    </application>

</manifest>